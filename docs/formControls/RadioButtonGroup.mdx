---
menu: Form Controls
name: RadioButtonGroup
route: /form-controls/radioButtonGroup
---

import { RadioButton } from "@class101/ui";
import { Playground, Props } from "docz";
import { RadioButtonGroup, handleAlert } from "./RadioButtonGroup";

# RadioButtonGroup

RadioButtonGroup 입니다.

## Props

<Props of={RadioButtonGroup} />

### Vertical Stacking

<Playground>
  <React.Fragment>
    <RadioButtonGroup
      value="test"
      stackingDirection="vertical"
      onChange={handleAlert}
    >
      <RadioButton value="test">Option1</RadioButton>
      <RadioButton value="test2">Option2</RadioButton>
    </RadioButtonGroup>
    <RadioButtonGroup
      stackingDirection="vertical"
      textAlign="center"
      onChange={handleAlert}
    >
      <RadioButton value="test">Option1</RadioButton>
      <RadioButton value="test2">Option2</RadioButton>
    </RadioButtonGroup>
    <RadioButtonGroup
      stackingDirection="vertical"
      showBorder={false}
      showDivider
      onChange={handleAlert}
    >
      <RadioButton value="test">Option1</RadioButton>
      <RadioButton value="test2">Option2</RadioButton>
    </RadioButtonGroup>
  </React.Fragment>
</Playground>

### Horizontal Stacking

<Playground>
  {() => {
    const handleAlert = (e) => {
      if (window) {
        window.alert(e);
      }
    };
    return (
      <React.Fragment>
        <RadioButtonGroup stackingDirection="horizontal" onChange={handleAlert}>
          <RadioButton value="test">Option1</RadioButton>
          <RadioButton value="test2">Option2</RadioButton>
        </RadioButtonGroup>
        <RadioButtonGroup
          stackingDirection="horizontal"
          showBorder={false}
          showDivider
          onChange={handleAlert}
        >
          <RadioButton value="test">Option1</RadioButton>
          <RadioButton value="test2">Option2</RadioButton>
        </RadioButtonGroup>
        <RadioButtonGroup
          stackingDirection="horizontal"
          showBorder={false}
          showDivider
          textAlign="center"
          onChange={handleAlert}
        >
          <RadioButton value="test">Option1</RadioButton>
          <RadioButton value="test2">Option2</RadioButton>
        </RadioButtonGroup>
      </React.Fragment>
    );
  }}
</Playground>
