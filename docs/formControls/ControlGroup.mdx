---
menu: Form Controls
name: ControlGroup
route: /form-controls/control-group
---

import { Playground, PropsTable } from 'docz';
import { Button, Select, ControlGroup, Input, ButtonSize, ButtonColor, Intent } from '@class101/ui';

# Control Group

각기 다른 컨트롤을 하나의 단위로 묶어서 보여줍니다.

## Basic usage

<Playground>
  <ControlGroup>
    <Input placeholder="Placeholder text" />
    <Button size={ButtonSize.LARGE}>Click me</Button>
  </ControlGroup>
  <br />
  <ControlGroup>
    <Input placeholder="Placeholder text" size="sm" />
    <Button>Click me</Button>
  </ControlGroup>
</Playground>

<Playground>
    <ControlGroup>
    <Input placeholder="Placeholder text" fill={false} />
    <Button size={ButtonSize.LARGE}>Click me</Button>
  </ControlGroup>
  <br />
  <ControlGroup>
    <Input placeholder="Placeholder text" size="sm" fill={false} />
    <Button>Click me</Button>
  </ControlGroup>
</Playground>

<Playground>
  <ControlGroup>
    <Select value="CandyCandyCandyCandyCandy" placeholder="Select one" options={['Apple', 'Banana', 'CandyCandyCandyCandyCandy']} size="sm" fill={false} />
    <Input placeholder="Placeholder text" size="sm" />
    <Input placeholder="Placeholder text" size="sm" />
  </ControlGroup>
  <br />
  <ControlGroup>
    <Select value="" placeholder="Select one" options={['Apple', 'Banana', 'Candy']} size="sm" fill={false} />
    <Input placeholder="Placeholder text" size="sm" />
    <Button>Click me</Button>
  </ControlGroup>
</Playground>

<Playground>
  <ControlGroup>
    <Select value="" placeholder="Select one" options={['Apple', 'Banana', 'Candy']} fill={false} />
    <Input placeholder="Placeholder text" />
    <Button size={ButtonSize.LARGE}>Click me</Button>
  </ControlGroup>
  <br />
  <ControlGroup>
    <Select value="" placeholder="Select one" options={['Apple', 'Banana', 'Candy']} fill={false} />
    <Input placeholder="Placeholder text" intent={Intent.WARNING} />
    <Button size={ButtonSize.LARGE}>Click me</Button>
  </ControlGroup>
  <br />
  <ControlGroup>
    <Select value="" placeholder="Select one" options={['Apple', 'Banana', 'Candy']} fill={false} />
    <Input placeholder="Placeholder text" intent={Intent.DANGER} />
    <Button size={ButtonSize.LARGE}>Click me</Button>
  </ControlGroup>
</Playground>