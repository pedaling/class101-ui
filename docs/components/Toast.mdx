---
menu: Components
name: Toast
route: /components/toast
---

import { Playground, PropsTable } from 'docz';
import { Component as Toast, ToastButton, UIContextProvider } from './Toast';

# Toast

오류, 알림 등을 알리기에 유용한 컴포넌트입니다. `Toaster.create`를 사용합니다.

## Props

<PropsTable of={Toast} />

## Basic usage

```tsx
import { Toaster } from '@class101-ui/core';

export const AppToaster = Toaster.create({
  position:
});
```

<Playground>
  <ToastButton />
</Playground>
