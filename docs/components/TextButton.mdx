---
menu: Components
name: TextButton
route: /components/text-button
---

import { Playground, PropsTable } from 'docz';
import { Icon, Colors, TextButton, ButtonColor, ButtonSize, Theme } from '@class101/ui';
import { PlayGroundContainer } from '../HelpComponents';

# TextButton

## Props

<PropsTable of={TextButton} />

## Basic usage

<Playground>
  <TextButton>Click me</TextButton>
</Playground>

## With color / theme

```ts
export enum ButtonColor {
  DEFAULT = 'default',
  ORANGE = 'orange',
  RED = 'red',
  BLUE = 'blue',
  WHITE = 'white',
}
```

<Playground>
  Default(Theme)
  <PlayGroundContainer>
    <TextButton>Click me</TextButton>
    <TextButton leftIcon={<Icon.Add />}>Click me</TextButton>
    <TextButton disabled>Click me</TextButton>
    <TextButton leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton loading>Click me</TextButton>
  </PlayGroundContainer>
  <PlayGroundContainer theme={Theme.dark}>
    <TextButton theme={Theme.dark}>Click me</TextButton>
    <TextButton theme={Theme.dark} leftIcon={<Icon.Add />}>
      Click me
    </TextButton>
    <TextButton theme={Theme.dark} disabled>
      Click me
    </TextButton>
    <TextButton theme={Theme.dark} leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton theme={Theme.dark} loading>
      Click me
    </TextButton>
  </PlayGroundContainer>
  <br />
  Orange
  <PlayGroundContainer>
    <TextButton color={ButtonColor.ORANGE}>Click me</TextButton>
    <TextButton color={ButtonColor.ORANGE} leftIcon={<Icon.Add />}>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.ORANGE} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.ORANGE} leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.ORANGE} loading>
      Click me
    </TextButton>
  </PlayGroundContainer>
  Red
  <PlayGroundContainer>
    <TextButton color={ButtonColor.RED}>Click me</TextButton>
    <TextButton color={ButtonColor.RED} leftIcon={<Icon.Add />}>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.RED} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.RED} leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.RED} loading>
      Click me
    </TextButton>
  </PlayGroundContainer>
  Blue
  <PlayGroundContainer>
    <TextButton color={ButtonColor.BLUE}>Click me</TextButton>
    <TextButton color={ButtonColor.BLUE} leftIcon={<Icon.Add />}>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.BLUE} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.BLUE} leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.BLUE} loading>
      Click me
    </TextButton>
  </PlayGroundContainer>
  White
  <PlayGroundContainer theme={Theme.dark}>
    <TextButton color={ButtonColor.WHITE}>Click me</TextButton>
    <TextButton color={ButtonColor.WHITE} leftIcon={<Icon.Add />}>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.WHITE} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.WHITE} leftIcon={<Icon.Add />} disabled>
      Click me
    </TextButton>
    <TextButton color={ButtonColor.WHITE} loading>
      Click me
    </TextButton>
  </PlayGroundContainer>
</Playground>

## With different sizes

```ts
export enum ButtonSize {
  LARGE = 'lg',
  MEDIUM = 'md',
  SMALL = 'sm',
}
```

<Playground>
  <PlayGroundContainer>
    <TextButton size={ButtonSize.LARGE}>Click me</TextButton>
    <TextButton size={ButtonSize.MEDIUM}>Click me</TextButton>
    <TextButton size={ButtonSize.SMALL}>Click me</TextButton>
  </PlayGroundContainer>
</Playground>

## With different icons

<Playground>
  <PlayGroundContainer>
    <TextButton leftIcon={<Icon.Heart />} size={ButtonSize.LARGE}>
      Click me
    </TextButton>
    <TextButton leftIcon={<Icon.Heart />} size={ButtonSize.MEDIUM}>
      Click me
    </TextButton>
    <TextButton leftIcon={<Icon.Heart />} size={ButtonSize.SMALL}>
      Click me
    </TextButton>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <TextButton rightIcon={<Icon.HeartOutline />} size={ButtonSize.LARGE}>
      Click me
    </TextButton>
    <TextButton rightIcon={<Icon.HeartOutline />} size={ButtonSize.MEDIUM}>
      Click me
    </TextButton>
    <TextButton rightIcon={<Icon.HeartOutline />} size={ButtonSize.SMALL}>
      Click me
    </TextButton>
  </PlayGroundContainer>
</Playground>

## Loading button

<Playground>
  <TextButton loading>Click me</TextButton>
</Playground>

## Anchor buttons

버튼과 똑같이 생겼지만 HTML a 태그로 구성되어 있습니다.

<Playground>
  <PlayGroundContainer>
    <TextButton href="/" target="_blank">
      Native A Tag
    </TextButton>
    <TextButton to="/" target="_blank">
      Link of React Router Dom
    </TextButton>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <TextButton href="/" target="_blank" disabled>
      Native A Tag
    </TextButton>
    <TextButton to="/" target="_blank" disabled>
      Link of React Router Dom
    </TextButton>
  </PlayGroundContainer>
</Playground>
