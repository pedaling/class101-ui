---
menu: Components
name: Button
route: /components/button
---

import { Playground, PropsTable } from 'docz';
import { Icon, Colors, Button } from '@class101/ui';

# Button

눌러서 제어하는 기능을 담당하는 컴포넌트입니다.

> **NOTE**
>
> - v4부터 Icon 컴포넌트와 함께 사용할 수 있도록 flex 기반의 스타일링을 지원하고 있습니다.
> - v4부터 textAlign 프로퍼티는 지원하지 않습니다.
> - v6부터 src를 지원하지 않습니다. 버튼의 아이콘은 Icon 컴포넌트를 사용해주세요.

## Props

<PropsTable of={Button} />

## Basic usage

<Playground>
  <Button>Click me</Button>
</Playground>

## With different sizes

<Playground>
  <Button size="lg">Click me</Button>
  <Button size="md">Click me</Button>
  <Button size="sm">Click me</Button>
  <Button size="xs">Click me</Button>
</Playground>

## With different colors

<Playground>
  <Button color="black" backgroundColor="pink">
    BLACKPINK
  </Button>
  <Button color="pink" backgroundColor="black">
    BLACKPINK
  </Button>
</Playground>

## With different icons

<Playground>
  <Button leftIcon={<Icon.Add fillColor={Colors.white} />}>Click me</Button>
  <Button rightIcon={<Icon.Add fillColor={Colors.white} />}>Click me</Button>
  <Button>
    <Icon.Add fillColor={Colors.white} size={16} />
    <span style={{ marginLeft: '4px' }}>Click me</span>
  </Button>
  <Button>
    <span style={{ marginRight: '4px' }}>Click me</span>
    <Icon.Add fillColor={Colors.white} size={16} />
  </Button>
</Playground>

## With fill

<Playground>
  <Button>Click me</Button>
  <Button fill>Click me</Button>
</Playground>

## Disabled button

<Playground>
  <Button disabled>Click me</Button>
  <Button rightIcon={<Icon.Heart fillColor={Colors.red600} />} disabled>
    Click me
  </Button>
</Playground>

## Loading button

<Playground>
  <Button loading>Click me</Button>
</Playground>

## Anchor buttons

버튼과 똑같이 생겼지만 HTML a 태그로 구성되어 있습니다.

<Playground>
  <Button href="/" target="_blank">
    Native A Tag
  </Button>
  <Button to="/" target="_blank" disabled>
    Link of React Router Dom
  </Button>
</Playground>
