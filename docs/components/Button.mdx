---
menu: Components
name: Button
route: /components/button
---

import { Playground, PropsTable } from 'docz';
import { Icon, Colors, Button, ButtonColor, ButtonSize, Theme } from '@class101/ui';
import { PlayGroundContainer } from '../HelpComponents';

# Button

눌러서 제어하는 기능을 담당하는 컴포넌트입니다.

> **NOTE**
>
> - v4부터 Icon 컴포넌트와 함께 사용할 수 있도록 flex 기반의 스타일링을 지원하고 있습니다.
> - v4부터 textAlign 프로퍼티는 지원하지 않습니다.
> - v6부터 src를 지원하지 않습니다. 버튼의 아이콘은 Icon 컴포넌트를 사용해주세요.
> - v7부터 color, backgroundColor는 지원하지 않을 예정입니다.

## Props

<PropsTable of={Button} />

## Basic usage

<Playground>
  <Button>Click me</Button>
</Playground>

## With color / theme

```ts
export enum ButtonColor {
  DEFAULT = 'DEFAULT',
  ORANGE = 'ORANGE',
  ORANGE_LIGHT = 'ORANGE_LIGHT',
  RED = 'RED',
  RED_LIGHT = 'RED_LIGHT',
  WHITE = 'WHITE',
}
```

<Playground>
  <PlayGroundContainer>
    <Button>Button</Button>
    <Button leftIcon={<Icon.Add />}>Button</Button>
    <Button disabled>Button</Button>
    <Button leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button loading>Button</Button>
  </PlayGroundContainer>
  <PlayGroundContainer theme={Theme.dark}>
    <Button theme={Theme.dark}>Button</Button>
    <Button theme={Theme.dark} leftIcon={<Icon.Add />}>
      Button
    </Button>
    <Button theme={Theme.dark} disabled>
      Button
    </Button>
    <Button theme={Theme.dark} leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button theme={Theme.dark} loading>
      Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.ORANGE}>CTA Button</Button>
    <Button color={ButtonColor.ORANGE} leftIcon={<Icon.Add />}>
      CTA Button
    </Button>
    <Button color={ButtonColor.ORANGE} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.ORANGE} leftIcon={<Icon.Add />} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.ORANGE} loading>
      CTA Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.ORANGE_LIGHT}>Button</Button>
    <Button color={ButtonColor.ORANGE_LIGHT} leftIcon={<Icon.Add />}>
      Button
    </Button>
    <Button color={ButtonColor.ORANGE_LIGHT} disabled>
      Button
    </Button>
    <Button color={ButtonColor.ORANGE_LIGHT} leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button color={ButtonColor.ORANGE_LIGHT} loading>
      Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.RED}>CTA Button</Button>
    <Button color={ButtonColor.RED} leftIcon={<Icon.Add />}>
      CTA Button
    </Button>
    <Button color={ButtonColor.RED} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.RED} leftIcon={<Icon.Add />} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.RED} loading>
      CTA Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.RED_LIGHT}>Button</Button>
    <Button color={ButtonColor.RED_LIGHT} leftIcon={<Icon.Add />}>
      Button
    </Button>
    <Button color={ButtonColor.RED_LIGHT} disabled>
      Button
    </Button>
    <Button color={ButtonColor.RED_LIGHT} leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button color={ButtonColor.RED_LIGHT} loading>
      Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.BLUE}>CTA Button</Button>
    <Button color={ButtonColor.BLUE} leftIcon={<Icon.Add />}>
      CTA Button
    </Button>
    <Button color={ButtonColor.BLUE} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.BLUE} leftIcon={<Icon.Add />} disabled>
      CTA Button
    </Button>
    <Button color={ButtonColor.BLUE} loading>
      CTA Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button color={ButtonColor.BLUE_LIGHT}>Button</Button>
    <Button color={ButtonColor.BLUE_LIGHT} leftIcon={<Icon.Add />}>
      Button
    </Button>
    <Button color={ButtonColor.BLUE_LIGHT} disabled>
      Button
    </Button>
    <Button color={ButtonColor.BLUE_LIGHT} leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button color={ButtonColor.BLUE_LIGHT} loading>
      Button
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer theme={Theme.dark}>
    <Button color={ButtonColor.WHITE}>Button</Button>
    <Button color={ButtonColor.WHITE} leftIcon={<Icon.Add />}>
      Button
    </Button>
    <Button color={ButtonColor.WHITE} disabled>
      Button
    </Button>
    <Button color={ButtonColor.WHITE} leftIcon={<Icon.Add />} disabled>
      Button
    </Button>
    <Button color={ButtonColor.WHITE} loading>
      Button
    </Button>
  </PlayGroundContainer>
</Playground>

## With size

```ts
export enum ButtonSize {
  LARGE = 'lg',
  MEDIUM = 'md',
  SMALL = 'sm',
  XSMALL = 'xs',
}
```

<Playground>
  <PlayGroundContainer>
    <Button size={ButtonSize.LARGE}>Click me</Button>
    <Button size={ButtonSize.MEDIUM}>Click me</Button>
    <Button size={ButtonSize.SMALL}>Click me</Button>
    <Button size={ButtonSize.XSMALL}>Click me</Button>
  </PlayGroundContainer>
</Playground>

## With icon

<Playground>
  <PlayGroundContainer>
    <Button leftIcon={<Icon.Heart />} size={ButtonSize.LARGE}>
      Click me
    </Button>
    <Button leftIcon={<Icon.Heart />} size={ButtonSize.MEDIUM}>
      Click me
    </Button>
    <Button leftIcon={<Icon.Heart />} size={ButtonSize.SMALL}>
      Click me
    </Button>
    <Button leftIcon={<Icon.Heart />} size={ButtonSize.XSMALL}>
      Click me
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button rightIcon={<Icon.HeartOutline />} size={ButtonSize.LARGE}>
      Click me
    </Button>
    <Button rightIcon={<Icon.HeartOutline />} size={ButtonSize.MEDIUM}>
      Click me
    </Button>
    <Button rightIcon={<Icon.HeartOutline />} size={ButtonSize.SMALL}>
      Click me
    </Button>
    <Button rightIcon={<Icon.HeartOutline />} size={ButtonSize.XSMALL}>
      Click me
    </Button>
  </PlayGroundContainer>
</Playground>

## With fill

<Playground>
  <PlayGroundContainer>
    <Button>Click me</Button>
    <Button fill>Click me</Button>
  </PlayGroundContainer>
</Playground>

## Disabled button

<Playground>
  <PlayGroundContainer>
    <Button disabled>Click me</Button>
    <Button rightIcon={<Icon.Heart />} disabled>
      Click me
    </Button>
  </PlayGroundContainer>
</Playground>

## Loading button

<Playground>
  <Button loading>Click me</Button>
</Playground>

## Anchor buttons

버튼과 똑같이 생겼지만 HTML a 태그로 구성되어 있습니다.

<Playground>
  <PlayGroundContainer>
    <Button href="/" target="_blank">
      Native A Tag
    </Button>
    <Button to="/" target="_blank">
      Link of React Router Dom
    </Button>
  </PlayGroundContainer>
  <PlayGroundContainer>
    <Button href="/" target="_blank" disabled>
      Native A Tag
    </Button>
    <Button to="/" target="_blank" disabled>
      Link of React Router Dom
    </Button>
  </PlayGroundContainer>
</Playground>

##

<Playground>
  <PlayGroundContainer>
    <Button size="xs" variant="default" theme={Theme.dark} leftIcon={<Icon.Heart />}>
      Click me
    </Button>
    <div style={{ display: 'flex' }}>
      <Button>Click me</Button>
      <Button fill color={ButtonColor.ORANGE}>
        Click me
      </Button>
    </div>
  </PlayGroundContainer>
</Playground>
