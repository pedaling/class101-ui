---
menu: Components
name: Pagination
route: /components/pagination
---

import { Playground, PropsTable } from 'docz';
import { Pagination } from '@class101/ui';
import { PlayGroundContainer, ThemeProvider } from '../docsComponents';

# Pagination

Pagination is a component used to display a long list divided by one page.

## Props

<PropsTable of={Pagination} />

## Basic usage

<Playground>
  <PlayGroundContainer>
    <Pagination currentPageIndex={0} totalPageIndex={1} onChange={number => alert(number)} />
  </PlayGroundContainer>
</Playground>
