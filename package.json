{
  "version": "7.0.0",
  "repository": "https://github.com/pedaling/class101-ui.git",
  "author": "배현승 <bae-unidev@nate.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna clean",
    "develop": "lerna run start --scope=ui --stream",
    "docs": "lerna run start --scope=ui-website --stream",
    "prepare": "lerna run prepare",
    "publish-packages": "lerna publish",
    "lint": "lerna run lint",
    "test": "exit 0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "name": "@class101/ui",
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}
